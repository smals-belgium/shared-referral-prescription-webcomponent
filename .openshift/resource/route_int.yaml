apiVersion: v1
kind: Route
metadata:
  labels:
    deploymentconfig: &deploymentconfig referral-prescription-web-components
  name: referral-prescription-web-components-ext
spec:
  host: referral-prescription.test.ext.vascloud.be
  path: /web-components
  to:
    kind: Service
    name: *deploymentconfig
    weight: 100
